## Packages
framer-motion | Page transitions and detailed animations for game phases
canvas-confetti | Celebration effects for winning team
@types/canvas-confetti | Types for confetti
clsx | Utility for conditional classes (standard in shadcn)
tailwind-merge | Utility for merging tailwind classes (standard in shadcn)

## Notes
WebSocket connects to /ws path
Game state is synced via WebSocket 'state_update' events
Session ID is stored in localStorage to handle reconnects
Mobile-first design approach strictly followed
